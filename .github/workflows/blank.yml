name: Build and Deploy
on:
  push:
    branches:
      - main  # se activará solo cuando hagas push a la rama 'main'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.5
        with:
          branch: gh-pages  # desplegará los archivos de la carpeta 'dist' a la rama 'gh-pages'
          folder: dist
